# Master resume drop folder

Place your real resume here as Markdown or plain text, then run:

```bash
python -m src.cli import-resume resumes/master/YOUR_RESUME.md
```

PDF import is not supported in v0.1 — convert PDF → Markdown/text first.

Do **not** copy `tests/fixtures/sample_resume.md` here; that file is synthetic test data only.
